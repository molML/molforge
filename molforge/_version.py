"""Single source of truth for the MolForge version.

Imported by ``molforge/__init__.py`` and read by ``setup.py`` so the package
version is defined in exactly one place. The version participates in the
pipeline configuration hash, so a hash match implies a version match.
"""

__version__ = "1.0.0"
